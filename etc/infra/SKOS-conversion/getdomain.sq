# Fetches the FIBO domains to be processed.
# We expect this to be configured in skosify.ttl
SELECT (GROUP_CONCAT (?domain) AS ?list)
WHERE { []   <http://www.edmcouncil.org/skosify#domain> ?domain}

